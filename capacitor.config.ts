import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.challengue.ionic',
  appName: 'Challenge',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
